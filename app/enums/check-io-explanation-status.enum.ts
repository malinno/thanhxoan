export enum CheckIOExplanationStatus {
  new = 'new',
  confirmed = 'confirmed',
  approved_first = 'approved_first',
  approved_second = 'approved_second',
  rejected = 'rejected',
  canceled = 'canceled',
}
