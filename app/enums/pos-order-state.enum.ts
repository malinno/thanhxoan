export enum PosOrderState {
  quotation = 'quotation',
  to_approved = 'to_approved',
  confirm = 'confirm',
  approved = 'approved',
  sale = 'sale',
  cancel = 'cancel',
  to_shipping = 'to_shipping',
  shipping = 'shipping',
}
